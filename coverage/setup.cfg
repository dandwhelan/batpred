[coverage:run]
# Plugin loading tests import modules from a temporary directory that is gone by the time the
# report is written, which otherwise fails the whole report with "No source for code".
omit=
    */site-packages/*
    */predbat_plugin_test_*/*
